import FormControl from './FormControl';
import FormGroup from './FormGroup';
import FormLabel from './FormLabel';

const Form = {
    Group: FormGroup,
    Label: FormLabel,
    Control: FormControl,
};

export default Form;
